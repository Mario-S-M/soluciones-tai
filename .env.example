# Credenciales del entorno LOCAL de desarrollo.
# Los puertos solo se publican en 127.0.0.1, no en la red.

POSTGRES_USER=tai
POSTGRES_PASSWORD=cambiame
POSTGRES_DB=soluciones_tai

# 8080 estaba ocupado por otro servidor PHP local al montar el entorno.
WEB_PORT=8081
DB_PORT=5432
PGADMIN_PORT=5050

PGADMIN_DEFAULT_EMAIL=admin@tai.local
PGADMIN_DEFAULT_PASSWORD=cambiame
